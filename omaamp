#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ ! -f "$SCRIPT_DIR/main.py" ]; then
    echo "omaamp: hittar inte main.py bredvid $0" >&2
    exit 1
fi

exec uv run --directory "$SCRIPT_DIR" python "$SCRIPT_DIR/main.py" "$@"
